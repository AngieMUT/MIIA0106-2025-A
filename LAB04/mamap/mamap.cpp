// mamap.cpp : This file contains the 'main' function. Program execution begins and ends there.
//

#include <iostream>
#include <string>
using namespace std;

// ????????????????????????????????????
class LineFollowRobot {
public:
	// ????????????????????????????????????????????????
	// ??????: isLeftBlack, isCenterBlack, isRightBlack, (true = black, false = not found)
	void control(bool isLeftBlack, bool isCenterBlack, bool isRightBlack) {
		cout << "censor status [left: " << isLeftBlack << ", center: " << isCenterBlack << 
			", right: " << isRightBlack << "] -> order:";

		// === ??????????????????? ===
		// ?????????????????????? ?????????????
		if (isLeftBlack) {
			turnLeft();
		}
		// ????????????????????? ????????????
		else if (isRightBlack) {
			turnRight();
		}
		// ?????????????????????? ?????????????
		else if (isCenterBlack) {
			moveForward();
		}
		// ???????????????? ???????
		else {
			stopCar();
		}
		cout << endl;
	}
private:
	// ???????????????????????????
	void turnLeft() {
		cout << " Turn Left ";
	}
	void turnRight() {
		cout << " Turn Right ";
	}
	void moveForward() {
		cout << " Move Forward ";
	}
	void stopCar() {
		cout << " Stop ";
	}
};

int main()
{
	LineFollowRobot myRobot;
	int leftInput, centerInput, rightInput;

	cout << "=== Program to control a line-following robot ===" << endl;
	cout << "please enter sensor values (1 for black, 0 for not found):" << endl;
	cout << "enter -1 to exit the program." << endl;

	while (true) {
		// ???????????????
		cout << "write sensor values (left center right) example '100': ";

		// ???????????????????????????????????????????
		if (!(cin >> leftInput)) break; 
		if (leftInput == -1) break; // ????????????? -1 ????????????

		cin >> centerInput >> rightInput;

		// ??????? int (0/1) ???? bool (false/true)
		bool sLeft = (leftInput == 1);
		bool sCenter = (centerInput == 1);
		bool sRight = (rightInput == 1);

		// ??????????????????????????????
		myRobot.control(sLeft, sCenter, sRight);

		cout << "----------------------------------------" << endl;
		}
	return 0;
	}


// Run program: Ctrl + F5 or Debug > Start Without Debugging menu
// Debug program: F5 or Debug > Start Debugging menu

// Tips for Getting Started: 
//   1. Use the Solution Explorer window to add/manage files
//   2. Use the Team Explorer window to connect to source control
//   3. Use the Output window to see build output and other messages
//   4. Use the Error List window to view errors
//   5. Go to Project > Add New Item to create new code files, or Project > Add Existing Item to add existing code files to the project
//   6. In the future, to open this project again, go to File > Open > Project and select the .sln file
